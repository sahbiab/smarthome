import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF111418);
  static const cardDark = Color(0xFF1C1F26);
  static const accent = Color(0xFF4A90E2);
  static const textLight = Colors.white;
  static const textMuted = Colors.white54;
}
